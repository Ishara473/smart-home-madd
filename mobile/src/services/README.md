# Services Directory

This directory handles external communication and data fetching abstractions.

## Subdirectory Responsibilities

- `firebase/`: Firebase SDK initializations, Firestore real-time snapshot listeners (`onSnapshot`), Storage references, and Cloud Functions invocation abstractions.
- `api/`: REST/WebSocket API clients and external network request wrappers.
